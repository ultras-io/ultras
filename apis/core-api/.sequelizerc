const path = require('path');

const rootPath = 'build/core/data';

module.exports = {
  config: path.resolve(`${rootPath}/config`, 'config.js'),
  'models-path': path.resolve(rootPath, 'models'),
  'seeders-path': path.resolve(rootPath, 'seeders'),
  'migrations-path': path.resolve(rootPath, 'migrations'),
};
