enum UserProfilePrivacyEnum {
  /**
   * User's profile is private.
   */
  private = 'private',

  /**
   * User's profile is public.
   */
  public = 'public',
}

export default UserProfilePrivacyEnum;
