import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: 24,
    height: 24,
  },
  commentsView: {
    position: 'absolute',
    justifyContent: 'center',
    width: '100%',
    height: 20,
    paddingHorizontal: 2,
  },
  comments: {
    fontWeight: 'bold',
    fontSize: 10,
    textAlign: 'center',
  },
});
