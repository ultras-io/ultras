import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    top: 4,
    right: -4,
  },
  withContainer: {
    alignSelf: 'flex-start',
  },
});
