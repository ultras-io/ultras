import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: '100%',
    paddingTop: 50, //todo safe area remove
  },
  text: {
    height: 40,
    padding: 12,
    fontWeight: '600',
  },
});
