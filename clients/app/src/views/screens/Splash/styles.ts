import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  logo: {
    fontFamily: 'MontserratAlternates',
    fontWeight: '500',
    fontSize: 64,
    lineHeight: 64,
    marginBottom: 210,
  },
});
