import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: '100%',
  },
  matchList: {
    paddingHorizontal: 15,
  },
  flatList: {
    paddingTop: 15,
  },
});
