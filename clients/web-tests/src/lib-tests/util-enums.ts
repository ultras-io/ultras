import { TeamTypesEnum, OrderEnum, MatchStatusesEnum, WinnerEnum } from '@ultras/utils';

export const runTest = () => {
  console.log('enums:', {
    TeamTypesEnum,
    OrderEnum,
    MatchStatusesEnum,
    WinnerEnum,
  });
};
