import { TeamTypesEnum, MatchStatusesEnum, OrderEnum } from '@ultras/utils';

export const runTest = () => {
  console.log('enums:', {
    TeamTypesEnum,
    MatchStatusesEnum,
    OrderEnum,
  });
};
